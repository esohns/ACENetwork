# libACENetworkConfig.cmake.in
#  LIBACENETWORK_INCLUDE_DIRS - include directories for FooBar
#  LIBACENETWORK_LIBRARIES    - libraries to link against

# Compute paths
get_filename_component (LIBACENETWORK_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set (LIBACENETWORK_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# library dependencies (contains definitions for IMPORTED targets)
if (NOT TARGET libACENetwork AND NOT LIBACENETWORK_BINARY_DIR)
include ("${LIBACENETWORK_CMAKE_DIR}/ACENetworkTargets.cmake")
endif ()

# These are IMPORTED targets created by libACENetworkTargets.cmake
set (LIBACENETWORK_LIBRARIES libACENetwork)

